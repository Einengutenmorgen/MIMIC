[pytest]
# Show short test summary info (-ra), and run quietly (-q)
addopts = -ra -q

# Define custom markers
markers =
    real_api: marks tests that make real API calls (deselect with '-m "not real_api"')

#